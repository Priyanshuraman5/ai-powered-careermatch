package com.careermatch.config;

// NOTE: bulk seed data (skills taxonomy, sample jobs, sample users) lives in
// src/main/resources/db/seed.sql and is executed automatically via spring.sql.init on startup
// (dev profile only). This class is a placeholder for any future programmatic seeding hooks
// (e.g. re-computing derived data after seed load).
public class DataSeedConfig {
}
